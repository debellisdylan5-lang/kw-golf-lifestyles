const clubs=[{name:'Calusa Pines',city:'Naples',type:'Invitation-only golf'},{name:'Tiburón Golf Club',city:'Naples',type:'Resort & residential'},{name:'Grey Oaks Country Club',city:'Naples',type:'Private club community'},{name:'The Club at Mediterra',city:'Naples',type:'Private residential club'}];
    const list=document.getElementById('clubList');function render(q=''){list.innerHTML='';clubs.filter(c=>c.name.toLowerCase().includes(q.toLowerCase())).forEach((c,i)=>{const b=document.createElement('button');b.className='club '+(i===0?'active':'');b.innerHTML=`<strong>${c.name}</strong><small>${c.city} • ${c.type}</small>`;b.onclick=()=>{document.querySelectorAll('.club').forEach(x=>x.classList.remove('active'));b.classList.add('active');document.getElementById('mapTitle').textContent=c.name;document.getElementById('mapText').textContent=`${c.city}, Florida • ${c.type} • Sample community profile`};list.appendChild(b)})}render();document.getElementById('clubSearch').addEventListener('input',e=>render(e.target.value));
    addEventListener('scroll',()=>{document.getElementById('nav').classList.toggle('scrolled',scrollY>40);document.getElementById('heroMedia').style.transform=`scale(1.08) translateY(${scrollY*.08}px)`},{passive:true});
    const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('in')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));